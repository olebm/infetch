node_modules
.next
out
dist
coverage
.env.local
.env*.local

# SQLite database + runtime data (keep directory structure via .gitkeep)
data/invoices/**
data/raw-text/**
data/ai-cache/**
data/sessions/**
data/logs/**
data/*.db
data/*.db-shm
data/*.db-wal
# Glitchtip-Webhook-Log — Fehlertitel/Culprits können PII enthalten
data/sentry-errors.jsonl
!data/**/.gitkeep
!data/.gitkeep

# Postgres dumps — NEVER commit (contain prod data / secrets)
*.dump
*.dump.*
*.sql.gz

# Build artifacts
tsconfig.tsbuildinfo

# Supabase CLI local artifacts
supabase/.temp/
supabase/.branches/

# macOS
.DS_Store

# Claude Code
.claude/

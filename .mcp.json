{
  "mcpServers": {
    "plane": {
      "command": "npx",
      "args": ["-y", "@makeplane/plane-mcp-server"],
      "env": {
        "PLANE_API_KEY": "${INFETCH_PLANE_API_KEY}",
        "PLANE_WORKSPACE_SLUG": "betaform",
        "PLANE_API_HOST_URL": "https://plane.betaform.io"
      }
    }
  }
}
